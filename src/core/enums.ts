export enum LogType {
	INFO = 'info',
	ERROR = 'error',
	WARN = 'warn',
	DEBUG = 'debug',
	HTTP = 'http',
	VERBOSE = 'verbose',
	SILLY = 'silly'
}

export enum NodeType {
	LOCAL = 'local',
	DEVELOPMENT = 'development',
	TEST = 'test',
	PRODUCTION = 'production'
}
